#ifndef TUI_COMPONENTS_TOPBAR_H
#define TUI_COMPONENTS_TOPBAR_H

/**
 * @brief Renders the top navigation bar of the TUI.
 * 
 * Displays the application title, current screen name, and key system statistics 
 * (FPS, CPU, MEM) fetched from the global state.
 */
void tui_RenderTopBar(void);

#endif // TUI_COMPONENTS_TOPBAR_H
