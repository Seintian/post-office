/**
 * @file keymap.c
 * @ingroup tui_input
 * @brief HOW: Map raw key events to high-level command enums.
 *
 * Strategy:
 *  - Lookup table keyed by (key, modifiers) -> command.
 *  - Fallback: emit UI_EVENT_KEY_TYPED for unmapped sequences.
 */

// Implementation will be added in subsequent milestones.
